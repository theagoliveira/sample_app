<!DOCTYPE html>
<html lang="en">
  <head>
    <%= render "layouts/rails_default" %>
    <%= render "layouts/shim" %>

    <title><%= full_title(yield(:title)) %></title>
  </head>

  <body>
    <%= render "layouts/header" %>

    <div class="container">
      <%= yield %>
    </div>
  </body>
</html>
